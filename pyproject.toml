[project]
name = "ai-novel"
version = "0.2.0"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "python-multipart",
  "python-docx",
  "reportlab",
  "ebooklib",
  "pyyaml",
  "httpx",
]

[tool.vercel]
entrypoint = "api.index:app"
