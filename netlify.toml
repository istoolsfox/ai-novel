[build]
base = "frontend"
command = "npm install && npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "22"
VITE_STATIC_DEMO = "true"

[context.production.environment]
VITE_STATIC_DEMO = "true"
