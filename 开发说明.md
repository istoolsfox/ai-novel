# AI Novel Workbench — 开发说明（fork 版）

> 这是从 `wlxb625/ai-novel` fork 到 `istoolsfox/ai-novel` 的项目，作为你的主项目。
> 目标：自动生成小说 + 章节管理的长篇小说创作系统，并接入阿里云百炼模型。

## 归属 / 来源

- **GitHub 仓库**：`https://github.com/istoolsfox/ai-novel`（fork）
- **上游**：`wlxb625/ai-novel`（MIT 开源，本地优先 AI 长篇小说工作台）
- **本地路径**：`/Users/jintunda/Desktop/My Person/workspace/01-projects/ai-novel/`
- **参考研究**：`03-resources/novel-research/`（4 个项目的只读 clone + 研究报告.md）

## 这个技术栈

FastAPI + React + SQLite + 独立 Worker。纯 Web 架构，后端标准 Python，前端 Vite/React。

已具备（上游就有，成熟）：
- **章节管理**：`chapters` / `chapter_versions` / `chapter_scores` / 章节定稿
- **多章节自动托管**：章节简报 → 初稿 → 章节契约 → 连续性检查 → 自动修复 → 复检 → 定稿 → 记忆编译
- **长篇一致性**：`character_states` / `character_knowledge`（知识边界）/ `narrative_debts`（叙事债务）/ `foreshadowing_states`（伏笔）
- **记忆编译**：`story_facts` / `relationship_states` / `story_items` / `item_ownership`
- **剧情规划**：`story_graph`（多线剧情图谱、影响传播、停滞检测、滚动章节计划）
- **世界线分叉**：从任意章节分叉，隔离正文与状态
- **安全**：本地 Fernet 加密 API Key、密钥轮换、升级前快照、自动回滚
- **可视化**：托管中心、剧情图谱、世界线比较、运行中心、安全中心、升级中心

## 阿里云百炼接入（已完成）

`.env`（已被 `.gitignore` 忽略，不进 git）：

```env
OPENAI_API_KEY=<百炼 API Key>
OPENAI_BASE_URL=https://llm-<workspace>.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
OPENAI_MODEL=qwen3.5-plus
```

实测：`POST /api/projects/{id}/ai/test-connection` 返回「远程模型连接成功」，百炼 qwen3.5-plus 可用。

## 启动

```bash
cd /Users/jintunda/Desktop/My Person/workspace/01-projects/ai-novel
python3 -m venv .venv && source .venv/bin/activate
pip install -r backend/requirements.txt
uvicorn app.main:app --app-dir backend --port 8080
```

打开前端（另行构建后）或直接用 API。

## 待办 / 下一步（在该项目上加新功能）

参考 `03-resources/novel-research/研究报告.md`，优先考虑：
1. 接百炼模型配置走通（已完成基础连通）
2. 「一键生成整本小说」编排（当前偏章节级）
3. 章节生成后质量评分门控（借鉴 autonovel 的 keep/retry + 平台期检测）
4. 记忆状态「版本化/可撤回」而非覆盖（借鉴 ai-novel 的 status 迁移）

## 状态

- [x] fork 到 istoolsfox
- [x] clone 到 01-projects
- [x] 后端依赖安装、启动验证
- [x] 阿里百炼模型连通验证（qwen3.5-plus）
- [x] 配置 .env（不进 git）
- [ ] 前端构建/联调
- [ ] 新功能开发
